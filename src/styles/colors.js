// 왼쪽 배경색
const primaryColor = "#3A8ACF";

// 오른쪽 배경색
const secondaryColor = "#EEE8A9";

// 로그인 박스
const contentBoxColor = "#d6b7a9";

const textColor = "#5C2720";

const actionColor = "#dc8562";

const customColors = {
  primaryColor,
  secondaryColor,
  contentBoxColor,
  textColor,
  actionColor,
};

export default customColors;
